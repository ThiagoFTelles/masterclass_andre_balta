name: masterclass_andre_balta
description: Flutter, Mobx, MVC, MVVM

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  mobx: ^0.4.0
  flutter_mobx: ^0.3.4
  provider: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.3.1
  mobx_codegen: ^0.3.11


flutter:

  uses-material-design: true